## How to install:
Open the PlatformIO terminal
Clear the memory:
```
pio run -t erase
```
Upload the webpage to the filesystem:
```
pio run -t uploadfs
```
Upload the code:
```
pio run -t upload
```


